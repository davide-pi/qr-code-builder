name: CI

on:
  push:
    branches:
      - "main"
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  pull_request:
    branches:
      - "main"

# Cancel outdated runs for the same branch/PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v4.7.0

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v4.7.0
        with:
          configFilePath: GitVersion.yml

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Set version
        run: "npm version ${{ steps.gitversion.outputs.fullSemVer }} --no-git-tag-version"

      - name: Build
        run: npm run build

      - name: Write deploy metadata
        run: |
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            echo "deploy_type=production" > deploy-info.env
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "deploy_type=preview" > deploy-info.env
          else
            echo "deploy_type=none" > deploy-info.env
          fi
          echo "version=${{ steps.gitversion.outputs.fullSemVer }}" >> deploy-info.env

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: "dist"
          path: "./dist"
          retention-days: 7

      - name: Upload deploy metadata
        uses: actions/upload-artifact@v7
        with:
          name: "deploy-info"
          path: "deploy-info.env"
          retention-days: 7
